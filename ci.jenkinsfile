#!groovy

pipeline{
  agent { node { label "build"}}

  stages{
    stage("build"){
      steps{
        script{
          mvnHome = tool "M2"
          sh "${mvnHome}/bin/mvn -v"
        }
      }
    }
  }  
}
      
